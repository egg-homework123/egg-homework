services:
  - type: web
    name: egg-homework
    runtime: python
    buildCommand: "pip install -r requirements.txt || true"
    startCommand: "python cloud_server.py"
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
